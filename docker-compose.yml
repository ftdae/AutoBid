services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: autobid
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - autobid_postgres_data:/var/lib/postgresql/data

volumes:
  autobid_postgres_data:
